# is basically a setting or a configuration file for (in this content)music app 
from django.apps import AppConfig


class MusicConfig(AppConfig):
    name = 'music' # tells the name of the app (ie name = 'music') also called namespacing 